# Content v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate the owner-validated content v2 (4 executive summaries, 6 projects with honest status badges, Staff-Engineer experience rewrites) into the master data, with schema evolution, DE regeneration, and typographic guard tests.

**Architecture:** The schema enum is widened first (backward compatible), then the canonical FR/EN data is replaced, then German is regenerated through the hardened offline pipeline together with the API-test expectation updates (atomic consistency), then the enum is tightened, then a style guard test locks the typographic rules.

**Tech Stack:** Existing monorepo (Bun 1.3.14, Zod, Elysia, bun test). No new dependencies.

## Global Constraints

- Branch: `feature/profile-engine-phase1` (continue on it; suite currently 44 pass).
- Content source of truth: `docs/superpowers/specs/2026-07-05-content-v2-blocks.json` (owner-validated verbatim; copy EXACTLY, no rewording).
- Typography: the em dash character `—` is FORBIDDEN in all data content (rule: replace with `:` or `-`). `[DRAFT]`/`[ENTWURF]` markers are forbidden.
- No numbers/metrics invented in experience highlights.
- `bun run translate` requires `ANTHROPIC_API_KEY`; load it from `C:/Users/delfa/git/Workspaces/AI_agents/_shared/.env_global` (never print or commit the value).
- Run all tests from the repo root. Commit style: Conventional Commits + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: Widen schema enums (status + product tag)

**Files:**
- Modify: `packages/schema/src/enums.ts` (Tag enum)
- Modify: `packages/schema/src/entities.ts` (Project status enum)
- Test: `packages/schema/src/resume.test.ts` (append tests)

**Interfaces:**
- Produces: `Tag` accepts `"product"`; `Project.status` accepts `"live" | "draft" | "building" | "concept"` (transitional: `draft` removed in Task 4).

- [ ] **Step 1: Append the failing tests** to `packages/schema/src/resume.test.ts`:

```ts
const validProject = {
  id: "x", name: "X",
  tagline: { en: "t", fr: "t", de: "t" },
  description: { en: "d", fr: "d", de: "d" },
  stack: [], tags: [], links: {}, status: "live", featured_for: [],
};

describe("status enum v2", () => {
  it("accepts building and concept", () => {
    expect(ProjectSchema.parse({ ...validProject, status: "building" }).status).toBe("building");
    expect(ProjectSchema.parse({ ...validProject, status: "concept" }).status).toBe("concept");
  });
});

describe("product tag", () => {
  it("accepts product", () => {
    expect(ProjectSchema.parse({ ...validProject, tags: ["product"] }).tags).toEqual(["product"]);
  });
});
```

Add `ProjectSchema` to the existing import from `./index` if not already imported, and `Tag` is not needed (goes through ProjectSchema).

- [ ] **Step 2: Run to verify failure**

Run: `bun test packages/schema/src/resume.test.ts`
Expected: FAIL (invalid enum value "building"; invalid tag "product").

- [ ] **Step 3: Widen the enums**

In `packages/schema/src/enums.ts`, add `"product"` to the `Tag` z.enum array (after `"aerospace"`).
In `packages/schema/src/entities.ts`, change the Project status line to:

```ts
    status: z.enum(["live", "draft", "building", "concept"]),
```

(`draft` stays temporarily so the not-yet-migrated data files keep validating; Task 4 removes it.)

- [ ] **Step 4: Run full suite to verify green**

Run: `bun test` — Expected: 47+ pass, 0 fail. Run: `bun run typecheck` — Expected: clean.

- [ ] **Step 5: Commit**

```bash
git add packages/schema
git commit -m "feat(schema): widen status enum (building, concept) and add product tag"
```

---

### Task 2: Canonical master data v2 (FR/EN)

**Files:**
- Modify: `data/master_data.fr.json`
- Test: `data/master_data.fr.test.ts` (update ids assertion)

**Interfaces:**
- Consumes: `docs/superpowers/specs/2026-07-05-content-v2-blocks.json` (verbatim source).
- Produces: `master_data.fr.json` with 4 new summaries, 6 projects (ids: profile-engine, harness, seomnix, ops-tools, artmap, omnis-agri, in this source order), 5 rewritten experiences. `de` keys ABSENT everywhere (unchanged rule).

- [ ] **Step 1: Update the failing test** — in `data/master_data.fr.test.ts`, replace the ids assertion:

```ts
  it("contains the six portfolio projects", () => {
    const r = ResumeInputSchema.parse(raw);
    const ids = r.projects.map((p) => p.id).sort();
    expect(ids).toEqual(["artmap", "harness", "omnis-agri", "ops-tools", "profile-engine", "seomnix"]);
  });
```

- [ ] **Step 2: Run to verify failure**

Run: `bun test data/master_data.fr.test.ts` — Expected: FAIL (3 old ids).

- [ ] **Step 3: Replace `executiveSummaries` and `projects`** in `data/master_data.fr.json` with the EXACT content of the annex file's `executiveSummaries` and `projects` keys (`docs/superpowers/specs/2026-07-05-content-v2-blocks.json`). Copy verbatim: the annex has only `en`/`fr` (correct for the canonical file).

- [ ] **Step 4: Replace the `safran` experience** with the annex's `experience_example_safran` object, keeping its `org`, `location`, `period`, `tags`, `domain` fields from the current file (the annex only redefines `role`, `summary`, `highlights` : merge, don't drop fields).

- [ ] **Step 5: Replace the 4 other experiences' `role`, `summary`, `highlights`** (keep org/location/period/tags/domain untouched) with:

**airbus-helicopters:**
```json
"role": { "en": "Software Architect - H175, NH90, AS332 Puma Programs", "fr": "Architecte logiciel - Programmes H175, NH90, AS332 Puma" },
"summary": { "en": "Owned Enovia VPM V4 platform reliability and industrialized the software delivery chain.", "fr": "Responsable de la fiabilité de la plateforme Enovia VPM V4 et de l'industrialisation de la chaîne de livraison logicielle." },
"highlights": [
  { "en": "Hardened the Enovia VPM V4 platform (environments, access rights, licensing, C++ customization), guaranteeing continuity for the program teams.", "fr": "Fiabilisation de la plateforme Enovia VPM V4 (environnements, droits d'accès, licences, customisation C++), garantissant la continuité de travail des équipes programmes." },
  { "en": "Industrialized the delivery chain (Gradle/Maven, Jenkins pipelines, JFrog Artifactory, Ansible automation), eliminating manual deployment errors.", "fr": "Industrialisation de la chaîne de livraison (Gradle/Maven, pipelines Jenkins, JFrog Artifactory, automatisation Ansible), éliminant les erreurs de déploiement manuelles." },
  { "en": "Reduced incidents and hardened software quality (SonarQube, JUnit, Ranorex, EggPlant, XRay) in direct support of the H175, NH90 and AS332 programs.", "fr": "Réduction des incidents et durcissement de la qualité logicielle (SonarQube, JUnit, Ranorex, EggPlant, XRay) en soutien direct des programmes H175, NH90 et AS332." },
  { "en": "Built Java services for CAD/PLM interoperability, streamlining exchanges between engineering offices.", "fr": "Développement de services Java d'interopérabilité CAO/PLM, fluidifiant les échanges entre bureaux d'études." }
]
```

**dassault-falcon:**
```json
"role": { "en": "PLM Engineer - Falcon 10X, 900LX, Multi Programs", "fr": "Ingénieur PLM - Programmes Falcon 10X, 900LX, Multi" },
"summary": { "en": "Migrated the PLM platform to AIX 7.2 on production-critical environments.", "fr": "Migration de la plateforme PLM vers AIX 7.2 sur des environnements de production critiques." },
"highlights": [
  { "en": "Executed the cutover plan end to end (Enovia VPM V4 upgrade packages, data-flow supervision, post-migration validation) with no interruption for the programs.", "fr": "Exécution du plan de bascule de bout en bout (packages Enovia VPM V4, supervision des flux, validation post-migration), sans interruption d'activité pour les programmes." },
  { "en": "Coordinated infrastructure and application teams, guaranteeing business continuity throughout the migration.", "fr": "Coordination des équipes infrastructure et applicatives, garantissant la continuité des opérations pendant toute la migration." }
]
```

**airbus-ds:**
```json
"role": { "en": "Developer - PLM Neo A32X & A35X Programs", "fr": "Développeur - Programmes PLM Neo A32X & A35X" },
"summary": { "en": "Extended the Dassault Systèmes 3DEXPERIENCE platform for the A32X and A35X programs.", "fr": "Extension de la plateforme 3DEXPERIENCE de Dassault Systèmes pour les programmes A32X et A35X." },
"highlights": [
  { "en": "Designed and integrated a 3DEXPERIENCE data model aligned with the client's business cases, accelerating platform adoption by the business teams.", "fr": "Conception et intégration d'un modèle de données 3DEXPERIENCE aligné sur les cas d'affaires du client, accélérant l'adoption de la plateforme par les métiers." },
  { "en": "Developed web services, widgets and JPOs, extending the platform to program-specific needs.", "fr": "Développement de webservices, widgets et JPOs, étendant la plateforme aux besoins spécifiques des programmes." },
  { "en": "Raised offshore (India) and internal teams' standards through training and coaching on development best practices.", "fr": "Montée en compétence des équipes offshore (Inde) et internes par formation et coaching aux bonnes pratiques de développement." }
]
```

**bombardier:**
```json
"role": { "en": "Aerospace Structures Engineer - CSeries (A220) Program", "fr": "Ingénieur structures aérospatiales - Programme CSeries (A220)" },
"summary": { "en": "Performed stress analysis on composite structures and built the supporting calculation tooling.", "fr": "Calculs de contraintes sur structures composites et développement de l'outillage de calcul associé." },
"highlights": [
  { "en": "Designed a new SQL database for composite materials, centralizing previously scattered test data.", "fr": "Conception d'une nouvelle base de données SQL pour les matériaux composites, centralisant des données d'essais jusque-là dispersées." },
  { "en": "Built a VB.NET stress-calculation tool and optimized fatigue-test computations, accelerating test campaigns on metallic structures.", "fr": "Développement d'un outil de calcul de contraintes en VB.NET et optimisation des calculs de fatigue, accélérant les campagnes d'essais des structures métalliques." }
]
```

- [ ] **Step 6: Sweep residual em dashes**

Run: `grep -n "—" data/master_data.fr.json`
Expected: no output. If any line appears, replace `—` with `:` (explanation) or `-` (separator) per the global rule.

- [ ] **Step 7: Run to verify green**

Run: `bun test data/master_data.fr.test.ts` — Expected: 2 pass.
Run: `bun test` — Expected: all pass (the i18n-based API tests still run on the OLD i18n file; that's expected until Task 3).

- [ ] **Step 8: Commit**

```bash
git add data/master_data.fr.json data/master_data.fr.test.ts
git commit -m "feat(data): content v2 - 6 projects, hook summaries, Staff-Engineer experiences"
```

---

### Task 3: Hardened translator prompt + DE regeneration + API test updates

**Files:**
- Modify: `scripts/generate-translations.ts` (system prompt)
- Regenerate: `data/master_data.i18n.json`
- Modify: `apps/api/src/profile.test.ts`, `apps/api/src/app.test.ts` (ordering expectations)

**Interfaces:**
- Consumes: `run()` and `anthropicTranslator` in `scripts/generate-translations.ts`; new FR/EN data from Task 2.
- Produces: `master_data.i18n.json` v2 (EN/FR/DE, 6 projects); API tests aligned with the new ordering: anthropic_dx head = harness, profile-engine, seomnix, ops-tools; artmap last; default role = artmap first; iot = omnis-agri first (unchanged).

- [ ] **Step 1: Harden the system prompt** in `scripts/generate-translations.ts` (replace the `system:` string):

```ts
      system:
        `You are a professional translator for a technical résumé. Translate the user's text into ${LANG_NAME[target]}. ` +
        `Preserve technical terms, product names, and proper nouns verbatim (e.g. Bun, LangGraph, Enovia, MCP). ` +
        `Translate job titles and degree names into idiomatic ${LANG_NAME[target]} conventions; never leave them in English. ` +
        `Use a consistent nominal style for CV bullet points. ` +
        `Never use the em dash character "—"; use ":" or "-" instead. ` +
        `Return ONLY the translated text, no quotes, no preamble.`,
```

- [ ] **Step 2: Verify the stub test still passes**

Run: `bun test scripts/generate-translations.test.ts` — Expected: 1 pass.

- [ ] **Step 3: Regenerate German** (bash, from repo root; never echo the key):

```bash
export ANTHROPIC_API_KEY="$(grep '^ANTHROPIC_API_KEY=' 'C:/Users/delfa/git/Workspaces/AI_agents/_shared/.env_global' | cut -d= -f2- | tr -d '\r' | tr -d '"')"
bun scripts/generate-translations.ts
```

Expected: `Wrote data/master_data.i18n.json`.

- [ ] **Step 4: Post-generation checks**

```bash
grep -c "—" data/master_data.i18n.json          # expected: 0 (grep exits 1)
grep -c "\[DRAFT\]\|\[ENTWURF\]" data/master_data.i18n.json  # expected: 0 (grep exits 1)
```

If either finds matches, fix the offending `de` values by hand (replace `—` per rule) and note it in the report.

- [ ] **Step 5: Update ordering expectations** in `apps/api/src/profile.test.ts` : replace the old "omnis-agri last" test with:

```ts
  it("orders anthropic_dx projects per spec (featured first, artmap last)", () => {
    const ids = buildProfile("anthropic_dx", "en", data).projects.map((p: any) => p.id);
    expect(ids.slice(0, 4)).toEqual(["harness", "profile-engine", "seomnix", "ops-tools"]);
    expect(ids[ids.length - 1]).toBe("artmap");
  });

  it("features artmap first for the default role", () => {
    expect(buildProfile("default", "en", data).projects[0].id).toBe("artmap");
  });
```

In `apps/api/src/app.test.ts`, the profile/build test asserting the LAST project id `"omnis-agri"` must now assert `"artmap"`.

- [ ] **Step 6: Full suite green**

Run: `bun test` — Expected: all pass (count ≥ 47). Run: `bun run typecheck` — Expected: clean.

- [ ] **Step 7: Commit**

```bash
git add scripts/generate-translations.ts data/master_data.i18n.json apps/api/src/profile.test.ts apps/api/src/app.test.ts
git commit -m "feat(data): regenerate DE v2 via hardened pipeline; align ordering tests"
```

---

### Task 4: Tighten the status enum (remove draft)

**Files:**
- Modify: `packages/schema/src/entities.ts`
- Test: `packages/schema/src/resume.test.ts` (append one test)

**Interfaces:**
- Produces: `Project.status` = `z.enum(["live", "building", "concept"])` (final form per spec §3).

- [ ] **Step 1: Append the failing test** to `packages/schema/src/resume.test.ts` (reuses `validProject` from Task 1):

```ts
  it("rejects the retired draft status", () => {
    expect(() => ProjectSchema.parse({ ...validProject, status: "draft" })).toThrow();
  });
```

- [ ] **Step 2: Run to verify failure**

Run: `bun test packages/schema/src/resume.test.ts` — Expected: FAIL (draft still accepted).

- [ ] **Step 3: Tighten the enum** in `packages/schema/src/entities.ts`:

```ts
    status: z.enum(["live", "building", "concept"]),
```

- [ ] **Step 4: Full suite green** (proves both data files are migrated)

Run: `bun test` — Expected: all pass. Run: `bun run typecheck` — Expected: clean.

- [ ] **Step 5: Commit**

```bash
git add packages/schema
git commit -m "feat(schema): retire draft status (data fully migrated)"
```

---

### Task 5: Typographic guard test

**Files:**
- Create: `data/master_data.style.test.ts`

**Interfaces:**
- Consumes: both data JSON files.
- Produces: a permanent CI guard for the typography rules.

- [ ] **Step 1: Write the test**

```ts
import { describe, expect, it } from "bun:test";
import fr from "./master_data.fr.json";
import i18n from "./master_data.i18n.json";

const FORBIDDEN = ["—", "[DRAFT]", "[ENTWURF]"] as const;
const FILES = [
  ["master_data.fr.json", fr],
  ["master_data.i18n.json", i18n],
] as const;

for (const [name, data] of FILES) {
  describe(`${name} typography`, () => {
    const text = JSON.stringify(data);
    for (const marker of FORBIDDEN) {
      it(`contains no "${marker}"`, () => {
        expect(text.includes(marker)).toBe(false);
      });
    }
  });
}
```

- [ ] **Step 2: Verify it passes** (guard, not regression: data was cleaned in Tasks 2-3)

Run: `bun test data/master_data.style.test.ts` — Expected: 6 pass.

- [ ] **Step 3: Prove the guard bites** (temporary, not committed): insert `—` into any `fr` string of `data/master_data.fr.json`, re-run the test (expected: FAIL), then `git checkout -- data/master_data.fr.json`.

- [ ] **Step 4: Full suite + commit**

Run: `bun test` — Expected: all pass.

```bash
git add data/master_data.style.test.ts
git commit -m "test(data): typographic guard - no em dash, no draft markers"
```

---

### Task 6: Final verification (controller-assisted)

**Files:** none new.

- [ ] **Step 1:** `bun test` (expect ≥ 50 pass, 0 fail) and `bun run typecheck` (clean), from repo root.
- [ ] **Step 2:** Smoke: `bun --cwd=apps/api run dev`, then `curl "http://localhost:3000/v1/profile/build?target_role=anthropic_dx&lang=fr"` : verify the hook summary text and the project order (harness, profile-engine, seomnix, ops-tools, omnis-agri, artmap). Kill the server.
- [ ] **Step 3 (controller-owned, not the implementer):** regenerate `docs/review-de.md` (back-translation of the new DE fields) for owner review.

## Self-Review

- **Spec coverage:** §3 schéma → Tasks 1+4 ; §4 contenu (annexe + 4 expériences + sweep) → Task 2 ; §5 ordre routage → Task 3 step 5 (tests figés) ; §6 pipeline DE + prompt durci + re-contrôles → Task 3 + Task 6 step 3 ; §7 tests → Tasks 1-5 ; §8 UPSKILLING → déjà livré (hors plan). ✅
- **Placeholder scan:** aucun TBD/TODO ; toutes les réécritures d'expériences sont fournies in extenso ; l'annexe est une source committée référencée par chemin exact. ✅
- **Type consistency:** `ProjectSchema`/`validProject` partagés entre Tasks 1 et 4 ; ids projets identiques entre Tasks 2, 3 et le spec §5. ✅
- **Ordre des tâches vérifié contre l'état intermédiaire :** l'enum garde `draft` jusqu'à ce que les DEUX fichiers data soient migrés (fr en Task 2, i18n en Task 3), puis se referme en Task 4. Aucune tâche ne laisse la suite rouge. ✅
